import React from 'react';
import {SafeAreaView} from 'react-native';
import {Home} from '.';

const Layout = () => {
  return (
    <SafeAreaView>
      <Home />
    </SafeAreaView>
  );
};

export default Layout;
